const settings = {
    worldSize: 600,
    tileSize: 64,
    FPS: 60,
    perlinSpread: 6,
}

const audioSettings = {
    backgroundMusicVolume: 0.03
}

const uiSettings = {
    expBarHeight: 0.03
}

const playerSettings = {
    expIncreasePerLevel: 1.1
}